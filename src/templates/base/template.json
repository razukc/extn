{
  "id": "base",
  "name": "Base Template",
  "description": "Shared Browser Preview features for all templates",
  "dependencies": [],
  "devDependencies": [
    "web-ext@^8.3.0",
    "concurrently@^9.1.0"
  ],
  "scripts": {
    "dev": "concurrently \"vite\" \"web-ext run --target chromium --arg=--auto-open-devtools-for-tabs --source-dir=./dist --config=./web-ext-config.mjs\""
  }
}
